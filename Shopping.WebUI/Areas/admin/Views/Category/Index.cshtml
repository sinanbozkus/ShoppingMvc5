@using Shopping.DataAccess.Models
@model List<Category>

<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h5>Category List</h5>
                    <div class="ibox-tools">
                        <a href="/admin/category/add" class="btn btn-primary">
                            New Category
                        </a>
                    </div>
                </div>
                <div class="ibox-content">

                    <table class="table">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Category Name</th>
                                <th>Description</th>
                                <th class="text-center">Active</th>
                                <th class="text-center">Operations</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Category item in Model)
                            {
                                <tr>
                                    <td>@item.Id</td>
                                    <td>@item.CategoryName</td>
                                    <td>@item.Description</td>
                                    <td class="text-center">
                                        @if (item.Active == true)
                                        {
                                            <i class="fa fa-lg fa-check-circle" aria-hidden="true" style="color:darkgreen;"></i>
                                        }
                                        else
                                        {
                                            <i class="fa fa-lg fa-times-circle" aria-hidden="true" style="color:darkred;"></i>
                                        }
                                    </td>
                                    <td class="text-center">
                                        <a href="/admin/category/edit/@item.Id" class="btn btn-xs btn-warning">
                                            <i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit
                                        </a>
                                        <a href="/admin/category/delete/@item.Id" class="btn btn-xs btn-danger">
                                            <i class="fa fa-times" aria-hidden="true"></i> Delete
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>
        </div>
    </div>
</div>